import { LocalizacionPipe } from './localizacion.pipe';

describe('LocalizacionPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizacionPipe();
    expect(pipe).toBeTruthy();
  });
});
